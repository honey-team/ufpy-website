{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"<code>ufpy</code> package","text":"","tags":["information"]},{"location":"#introduction","title":"Introduction","text":"<p>Ufpy (Useful Python) - is a package for simplifying Python programs using many useful features.</p> <p>Now Ufpy has these features:</p> <ul> <li><code>UDict</code>.</li> <li><code>uSTL</code> package.<ul> <li><code>Stack</code>.</li> </ul> </li> <li>Generators of classes methods:<ul> <li><code>cmp_generator</code></li> <li><code>r_generator</code></li> <li><code>i_generator</code></li> </ul> </li> <li>many protocols for type hinting.</li> <li>many type alias for type hinting.</li> </ul>","tags":["information"]},{"location":"#installing","title":"Installing","text":"<p>To install <code>ufpy</code>, you need <code>python 3.12+</code> and <code>pip</code>. After installing, use this command in your <code>cmd</code>/<code>bash</code> terminal:</p> You have one python versionSeveral versions WIndowsLinux <pre><code>pip install ufpy\n</code></pre> <p>In Linux there isn't abillity to install packages globally (except Fedora, you can use <code>pip3</code>). But you can create <code>venv</code>! <pre><code># If you haven't venv package -&gt; install it\nsudo apt install python3.12-venv # On Debian, replace 3.12 with your version\nsudo dnf install python3.12-venv # On Fedora\n\n# Create venv\npython3.12 -m venv venv # replace 3.12 with your version\n</code></pre> Then activate it and install <code>ufpy</code>! <pre><code>source venv/bin/activate\npip install ufpy\n</code></pre></p> WindowsLinux <pre><code>python -3.12 -m pip install ufpy\n# or\npy -3.12 -m pip install ufpy\n</code></pre> <p>Create <code>venv</code> with specific python version. <pre><code># If you haven't venv package -&gt; install it\nsudo apt install python3.12-venv # On Debian, replace 3.12 with your version\nsudo dnf install python3.12-venv # On Fedora\n\n# Create venv\npython3.12 -m venv venv # replace 3.12 with your version\n</code></pre> Then activate it and install <code>ufpy</code>! <pre><code>source venv/bin/activate\npip install ufpy\n</code></pre></p>","tags":["information"]},{"location":"#importing-and-writing-some-code","title":"Importing and writing some code","text":"<p>After installing, you can use <code>ufpy</code> package in all your projects with <code>3.12+</code> python version. Just import <code>ufpy</code> or import certain classes, functions and variables.</p> <pre><code>import ufpy\nfrom ufpy import UDict\n</code></pre> <p>Enjoy!</p>","tags":["information"]},{"location":"#about-the-site","title":"About the site","text":"<p>Site was made using <code>mkdocs</code> with <code>mkdocs material</code> theme. You can use search: just click <code>Search</code> text input at the top of the page or use the S or F hotkeys. You can switch themes using the button on the left and access the <code>ufpy</code> repository using the button on the right.</p>","tags":["information"]},{"location":"#contribute","title":"Contribute","text":"<p>You can also contribute to <code>ufpy</code> package or <code>ufpy</code> docs site. Just  go to <code>ufpy</code> repository using button in right-top of page. For contributing to site you can go to <code>ufpy-website</code> repository.</p>","tags":["information"]},{"location":"type_checking/protocols/","title":"Protocols","text":"","tags":["type check"]},{"location":"type_checking/type_alias/","title":"Type alias","text":"","tags":["type check"]},{"location":"useful_classes/udict/","title":"class UDict[KT, VT, CDV]","text":"<pre><code>class UDict(dictionary: AnyDict[KT, VT]) # (1)!\nclass UDict(dictionary: AnyDict[KT, VT], *, default: CDV)\nclass UDict(**kwargs: VT)\nclass UDict(*, default: CDV, **kwargs: VT)\n</code></pre> <ol> <li> <p><code>AnyDict[KT, VT] = LikeDict[KT, VT] | dict[KT, VT]</code>. In UDict </p> <p>Note</p> <p><code>LikeDict[KT, VT]</code> is type which has <code>__getitem__</code>, <code>__setitem__</code>, <code>__delitem__</code> and <code>get()</code> methods. <code>UDict</code> is <code>LikeDict</code>.</p> <p>Read about AnyDict and LikeDict</p> </li> </ol> <p>UDict as generic</p> <p>You can use <code>UDict</code> as <code>Generic</code>, because of it, there are 3 <code>TypeVar</code>s: KT, VT, CDV.</p> <p>KT and VT are key type and value type. In inbuilt <code>dict</code> there are KT and VT type vars. CDV is class default value.</p> <p>In this documentation KT, VT and CDV will be used in methods.</p> <p>Create UDict object. If <code>default</code>, when not existent keys is given in getting item, method will return <code>default</code>.</p> <p>Example</p> <pre><code>d = UDict(hello=world, hi=python, default=10)\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#property-settable-dictionary-dictkt-vt","title":"(property, settable) dictionary: dict[KT, VT]","text":"<p>UDict's dictionary.</p> <p>Example</p> <pre><code>print(d.dictionary)\nd.dictionary = {4: 'world'}\n</code></pre> <p>Tip</p> <p>You can use UDict to set dictionary</p> <p>Example</p> <pre><code>d.dictionary = UDict({1: 7})\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#property-settable-keys-listkt","title":"(property, settable) keys: list[KT]","text":"<p>UDict's keys</p> <p>Example</p> <pre><code>print(d.keys)\nd.keys = [1, 2]\n</code></pre> <p>Tip</p> <p>You can use tuples to set keys</p> <p>Example</p> <pre><code>d.keys = 1, 2\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#property-settable-values-listvt","title":"(property, settable) values: list[VT]","text":"<p>UDict's values</p> <p>Example</p> <pre><code>print(d.values)\nd.values = [7, 2]\n</code></pre> <p>Tip</p> <p>You can use tuples to set values</p> <p>Example</p> <pre><code>d.values = 7, 2\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#property-settable-items-listtuplekt-vt","title":"(property, settable) items: list[tuple[KT, VT]]","text":"<p>UDict's items.</p> <p>Example</p> <pre><code>print(d.items)\nd.items = [(1, 7), (2, 2)]\n</code></pre> <p>Tip</p> <p>You can use tuples to set items or you can use tuples or lists with lists</p> <p>Example</p> <pre><code>d.items = (1, 7), (2, 2)\nd.items = [1, 7], [2, 2]\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#property-settable-default-cdv","title":"(property, settable) default: CDV","text":"<p>UDict's default value</p> <p>Example</p> <pre><code>print(d.default)\nd.default = 'null'\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#reverse-udictkt-vt-cdv","title":"reverse() -&gt; UDict[KT, VT, CDV]","text":"<p>Reverses UDict and returns it. (1)</p> <ol> <li> <p>How UDict is being reversing?</p> <p>Just is being reversing items <code>u{'hello': 1, 'hi': 2}</code> -&gt; <code>u{'hi': 2, 'hello': 1}</code> (reversed)</p> </li> </ol> <p>Warning</p> <p><code>reverse()</code> edits UDict. If you don't want to reverse UDict use <code>reversed()</code> method instead.</p> <p>Example</p> <pre><code>d.reverse()\nprint(d) # prints reversed UDict\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#reversed-udictkt-vt-cdv","title":"reversed() -&gt; UDict[KT, VT, CDV]","text":"<p>Returns reversed UDict</p> <p>Example</p> <pre><code>print(d.reversed())\n</code></pre> <p>Get reversed UDict with inbuilt <code>reversed()</code> and <code>~</code> operator</p> <p>You can get reversed UDict with inbuilt <code>reversed()</code> and with invert operator (<code>~</code>).</p> <p>Example</p> <pre><code>print(~d)\nprint(reversed(d))\nprint(d.reversed() == reversed(d)) # True\n</code></pre> <p>Read more about <code>reversed()</code> and <code>~</code> support in UDict</p>","tags":["useful class"]},{"location":"useful_classes/udict/#sort-udictkt-vt-cdv","title":"sort() -&gt; UDict[KT, VT, CDV]","text":"<p>Sorts UDict and returns it. (1)</p> <ol> <li> <p>How UDict is being sorting?</p> <p>Just are being sorting items by keys. <code>u{'b': 1, 'a': 2}</code> -&gt; <code>u{'a': 2, 'b': 1}</code> (sorted)</p> </li> </ol> <p>Warning</p> <p><code>sort()</code> edits UDict. If you don't want to sort UDict use <code>sorted()</code> method instead.</p> <p>Example</p> <pre><code>print(d.sort())\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#sorted-udictkt-vt-cdv","title":"sorted() -&gt; UDict[KT, VT, CDV]","text":"<p>Returns sorted UDict</p> <p>Example</p> <pre><code>print(d.sorted())\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#get","title":"get()","text":"<pre><code>def get(*, key: KT) -&gt; VT | CDV\ndef get(*, key: KT, default: DV) -&gt; VT | DV\ndef get(*, index: int) -&gt; VT | CDV\ndef get(*, index: int, default: DV) -&gt; VT | DV\ndef get(*, value: VT) -&gt; KT | CDV\ndef get(*, value: VT, default: DV) -&gt; KT | DV\n</code></pre> <p>Using more than 1 argument</p> <p>If you use 0 or 2 or 3 of this arguments (<code>key</code>, <code>index</code>, <code>value</code>), method will raise <code>ValueError</code></p> <p>Arguments:</p>","tags":["useful class"]},{"location":"useful_classes/udict/#key-kt","title":"<code>key: KT</code>","text":"<p>UDict value's key to find.</p> <p>Example</p> <pre><code>print(d.get(key='key')) # same that d['key']\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#index-int","title":"<code>index: int</code>","text":"<p>UDict value's index to find</p> <p>Warning</p> <p>Indexes are starting from 1. Index of first element of UDict is 1.</p> <p><code>index</code> argument more than UDict length</p> <p>If you use <code>index</code> argument make sure that <code>index</code> are less than UDict length. Otherwise <code>get()</code> will raise <code>IndexError</code></p> <p>Example</p> <pre><code>print(d.get(index=2)) # second value of UDict\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#value-vt","title":"<code>value: VT</code>","text":"<p>UDict key's value to find</p> <p>Example</p> <pre><code>print(d.get(value=1)) # if d = UDict{'hello': 1}, this will be 'hello'\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#default-dv","title":"<code>default: DV</code>","text":"<p>Default value, if the result was not found. If not provided, then <code>default</code> is <code>UDict.default</code> property.</p> <p>Example</p> <pre><code>d = UDict(hello=1, hi=2, default=-1)\nprint(d.get(key='hell')) # -1\nprint(d.get(key='hell', default=None)) # None\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#is_empty-bool","title":"is_empty() -&gt; bool","text":"<p>Returns <code>True</code> if the UDict is empty (<code>len(UDict) == 0</code>), otherwise returns <code>False</code>.</p> <p>Example</p> <pre><code>    d = UDict(hello=1)\n    d2 = UDict()\n    print(d.is_empty()) # False\n    print(d2.is_empty()) # True\n</code></pre> <p>Convert UDict to <code>bool</code></p> <p>You can convert UDict to <code>bool</code> or use UDict in <code>if</code> statement. If <code>UDict.is_empty() == True</code> then <code>bool(UDict)</code> is False and by contrast. Same in <code>if</code> because of <code>if x</code> is the equivalent of <code>if bool(x)</code></p> <p>Example</p> <pre><code># d is from code above\nprint(bool(d)) # True\nif d:\n    print(\"d is True\") # d is True\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#magic-methods","title":"Magic methods","text":"<p>Currently, UDict supports all these magic methods:</p>","tags":["useful class"]},{"location":"useful_classes/udict/#__call__func-callablekt-vt-vt-udictkt-vt-cdv","title":"__call__(func: Callable[[KT, VT], VT]) -&gt; UDict[KT, VT, CDV]","text":"<p>Returns new UDict, but all values generated with <code>func</code> function. First argument: key, second: value.</p> <p>Arguments:</p>","tags":["useful class"]},{"location":"useful_classes/udict/#func-kt-vt-vt","title":"<code>func: (KT, VT) -&gt; VT</code>","text":"<p>First argument of function is key, second is value. Returns new value</p> <p>Example</p> <pre><code>def f(k, v):\n    return v * 2\nd = d(f) # multiply all values by 2\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__neg__-udictkt-int-float-cdv","title":"__neg__() -&gt; UDict[KT, int | float, CDV]","text":"<p>Negates all values (if they support the <code>-</code> operator) to their opposite numbers.</p> <p>Example</p> <pre><code>d = UDict(hello=1, hi=-2)\nprint(-d) # u{'hello': -1, 'hi': 2}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__invert__-udictkt-vt-cdv","title":"__invert__() -&gt; UDict[KT, VT, CDV]","text":"<p>The equivalent of <code>reversed()</code></p> <p>Example</p> <pre><code>print(~d)\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__reversed__-udictkt-vt-cdv","title":"__reversed__() -&gt; UDict[KT, VT, CDV]","text":"<p>The equivalent of <code>reversed()</code></p> <p>Example</p> <pre><code>print(reversed(d))\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__getitem__key-kt-int-slice-udictkt-vt-dv-vt","title":"__getitem__(key: KT | int | slice) -&gt; UDict[KT, VT, DV] | VT","text":"<p>Returns the value for a single key, or a UDict for multiple keys provided with a slice.</p> <p>Indexes starting at 1.</p> <p>Arguments:</p>","tags":["useful class"]},{"location":"useful_classes/udict/#key-kt-int-slice","title":"<code>key: KT | int | slice</code>","text":"<p>Value's key or index to get or values's indexes slice to get.</p> <p>Example</p> <pre><code>print(d['hello'])\nprint(d[1:2])\n</code></pre> <p>Using indexes</p> <p>You can also use indexes in <code>__getitem__()</code>.</p> <p>Failure</p> <p>Keep in mind that indexes are using after the keys with the given value were not found.</p> <p>Example</p> <p>If you have <code>1</code> key, <code>d[1]</code> syntax will use <code>1</code> how key, not index. If you want to use index in all ways, use <code>get()</code> instead.</p> <p>Example</p> <pre><code>d = UDict(hello=1, hi=9)\nprint(d[2]) # 9\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__setitem__key-kt-int-slice-value-vt-listvt-tuplevt","title":"__setitem__(key: KT | int | slice, value: VT | list[VT] | tuple[VT])","text":"<p>Sets the value or values for the given key or keys.</p>","tags":["useful class"]},{"location":"useful_classes/udict/#key-kt-int-slice_1","title":"<code>key: KT | int | slice</code>","text":"<p>Value's key or keys to set. This argument is the same with <code>key</code> argument in <code>__getitem__()</code> method</p>","tags":["useful class"]},{"location":"useful_classes/udict/#value-vt-listvt-tuplevt","title":"<code>value: VT | list[VT] | tuple[VT]</code>","text":"<p>Value or values to set.</p> <p>Example</p> <pre><code>d = UDict(hello=2, hi=1)\nd[2] = 'hello'\nprint(d['hi']) # 'hello'\nd[:] = 'hello', 'world'\nprint(d) # u{'hello': 'hello', 'hi': 'world'}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__delitem__key-kt-int-slice","title":"__delitem__(key: KT | int | slice)","text":"<p>Deletes items with the given key or keys.</p>","tags":["useful class"]},{"location":"useful_classes/udict/#key-kt-int-slice_2","title":"<code>key: KT | int | slice</code>","text":"<p>Item's key or keys to delete. This argument is the same with <code>key</code> argument in <code>__getitem__()</code> method.</p>","tags":["useful class"]},{"location":"useful_classes/udict/#__len__-int","title":"__len__() -&gt; int","text":"<p>Returns the length of the UDict.</p> <p>Example</p> <p>d = UDict(hello=1, hi=2) print(len(d)) # 2</p>","tags":["useful class"]},{"location":"useful_classes/udict/#__iter__-iteratortuplekt-vt","title":"__iter__() -&gt; Iterator[tuple[KT, VT]]","text":"<p>Iterate over the UDict. The equivalent of <code>items.__iter__()</code>.</p> <p>Example</p> <pre><code>for k, v in d:\n    print(f\"Key: {k}\\nValue: {v}\") # Prints all keys and its values.\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__bool__-bool","title":"__bool__() -&gt; bool","text":"<p>Returns whether the UDict is not empty. The equivalent of <code>not is_empty()</code></p> <p>Example</p> <pre><code>print(bool(d))\nif d:\n    print(\"D is not empty!\")\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__contains__item-tuplekt-vt-listkt-vt-kt-bool","title":"__contains__(item: tuple[KT, VT] | list[KT | VT] | KT) -&gt; bool","text":"<p>Checks whether the item or key is in the UDict.</p> <p>Arguments:</p>","tags":["useful class"]},{"location":"useful_classes/udict/#item-tuplekt-vt-listkt-vt-kt","title":"item: tuple[KT, VT] | list[KT | VT] | KT","text":"<p>Item or item's key.</p> <p>Example</p> <pre><code>if ('key', 'value') in d: ...\nif ['key', 'value'] in d: ...\nif 'key' in d: ...\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__repr__-str","title":"__repr__() -&gt; str","text":"<p>Returns the string representation of the UDict. This allows the UDict to be used in <code>print()</code> and <code>repr()</code>.</p> <p>Example</p> <pre><code>d = UDict(hello=1)\nprint(d) # u{'hello': 1}\nprint(repr(d)) # Same\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__hash__-int","title":"__hash__() -&gt; int","text":"<p>Returns the hash for <code>repr(UDict)</code>. The equivalent of <code>repr(UDict).__hash__()</code>.</p>","tags":["useful class"]},{"location":"useful_classes/udict/#__cmp__other-dictkt-vt-udictkt-vt-cdv-int","title":"__cmp__(other: dict[KT, VT] | UDict[KT, VT, CDV]) -&gt; int","text":"<p>Used by <code>@cmp_generator</code>, which generates comparison magic methods like <code>==</code>, <code>!=</code>, <code>&gt;</code>, <code>&gt;=</code>, <code>&lt;</code>, <code>&lt;=</code> operators.</p> <p>Comparing UDicts involves comparing their lengths, except for <code>__eq__()</code>.</p> <p>Example</p> <pre><code>d = UDict(hello=1)\nd2 = UDict(hello=1, hi=2)\nprint(d != d2) # True\nprint(d &lt; d2) # True\nprint(d &gt; d2) # False\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__eq__other-dictkt-vt-udictkt-vt-cdv-bool","title":"__eq__(other: dict[KT, VT] | UDict[KT, VT, CDV]) -&gt; bool","text":"<p>Checks whether UDicts are the same. (Overrides generated by <code>@cmp_generator</code> magic method)</p> <p>Example</p> <pre><code>d = UDict(hello=1)\nd2 = UDict(hello=1, hi=2)\nprint(d == d2) # False\nprint(d == d) # True\n</code></pre> <p>Tip</p> <p>You can use dict to compare with UDict. Method will automatically generate UDict from this dict <pre><code>print(d == {'hello': 1}) # True\n</code></pre></p>","tags":["useful class"]},{"location":"useful_classes/udict/#__add__other-dictkt-vt-udictkt-vt-cdv-udictkt-vt-cdv","title":"__add__(other: dict[KT, VT] | UDict[KT, VT, CDV]) -&gt; UDict[KT, VT, CDV]","text":"<p>Adds the dictionary or UDict's dictionary to the UDict's dictionary. This method also has <code>r</code> and <code>i</code> versions (<code>+=</code>).</p> <p>Example</p> <pre><code>print(d + {'hi': 2})\nprint({'hi': 2} + d)\nd += {'hi': 2}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__sub__other-dictkt-vt-udictkt-vt-cdv-udictkt-vt-cdv","title":"__sub__(other: dict[KT, VT] | UDict[KT, VT, CDV]) -&gt; UDict[KT, VT, CDV]","text":"<p>Subtracts the dictionary or UDict's dictionary from the UDict's dictionary. This method also has <code>r</code> and <code>i</code> versions (<code>-=</code>).</p> <p>Example</p> <pre><code>d = UDict(hello=1, hi=2)\nprint(d - {'hello': 1}) # u{'hi': 2}\nd -= {'hello': 1}\nprint(d) # Same\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__mul__other-dictkt-float-int-udictkt-float-int-dv-float-int-udictkt-supportsmul-cdv","title":"__mul__(other: dict[KT, float | int] | UDict[KT, float | int, DV] | float | int) -&gt; UDict[KT, SupportsMul, CDV]","text":"<p>Multiply all values by <code>other</code>, if <code>other</code> is <code>int</code> or <code>float</code>. Multiply values with keys equals to <code>other</code> keys by <code>other</code> values, if <code>other</code> is <code>dict</code> or UDict. This method also have <code>r</code> and <code>i</code> version (<code>*=</code>)</p> <p>Warning</p> <p>Make sure that your UDict's values supports multiply operator (<code>*</code>)</p> <p>Example</p> <pre><code>d = UDict(hello=1, hi=2)\nprint(d * 2) # {'hello': 2, 'hi': 4}\nprint(d * {'hi': 3}) # {'hello': 1, 'hi': 6}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#__truediv__other-dictkt-float-int-udictkt-float-int-dv-float-int-udictkt-supportstruediv-cdv","title":"__truediv__(other: dict[KT, float | int] | UDict[KT, float | int, DV] | float | int) -&gt; UDict[KT, SupportsTrueDiv, CDV]","text":"<p>Same that <code>__mul__()</code>, but with divide operator (<code>/</code>).</p> <p>Warning</p> <p>Make sure that your UDict's values supports divide operator (<code>/</code>)</p> <p>Example</p> <pre><code>d = UDict(hello=1, hi=2)\nprint(d / 2) # {'hello': 0.5, 'hi': 1}\nprint(d / {'hi': 4}) # {'hello': 1, 'hi': 0.5}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#examples","title":"Examples","text":"","tags":["useful class"]},{"location":"useful_classes/udict/#init","title":"Init","text":"<p>Example</p> <pre><code>d = UDict({'id': 2, 'content': 'hello, world'})\n# for string keys you can use this way:\nd = UDict(id=2, content='hello, world')\n</code></pre> <p>Example</p> <p>You can also define <code>default</code> value for items when you use item's getter: <pre><code>d = UDict(id=2, content='hello, world', default=0)\n</code></pre></p>","tags":["useful class"]},{"location":"useful_classes/udict/#get-items","title":"Get items","text":"<p>For getting items you should use the way you use in lists and dicts: use <code>UDict[key]</code> syntax:</p> <p>Example</p> <pre><code>d['id'] # 2\n</code></pre> <p>Tip</p> <p>You can also use index of key or slice of indexes of keys:</p> <p>Warning</p> <p>In this class first index is 1</p> <p>Example</p> <pre><code>d[1] # 2\nd[:] # u{'id': 2, 'content': 'hello, world'} (UDict object)\n</code></pre> <p>You can also use <code>get()</code> method. You can get item by its key using <code>key</code> kwarg, you can get item by its index using <code>index</code> kwarg, and you get item key by its value using <code>value</code> kwarg.</p> <p>You can define <code>default</code> only for this calling</p> <pre><code>d = UDict({2: 3, 1: 4}, default=None)\nd[2] # 3\nd.get(index=2) # 4\nd.get(key=1) # also 4\nd.get(value=3) # 2\n\nd.get(key=3) # None\nd.get(key=3, default='null') # 'null'\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#set-items","title":"Set items","text":"<p>For setting items you should use the way you use in lists and dicts: use <code>UDict[key] = value</code> syntax:</p> <p>Example</p> <pre><code>d['id'] = 3\n</code></pre> <p>You can use indexes and slices</p> <pre><code>d[1] = 2\nd[2:6:2] = 8\nd[:3] = 1, 2, 3\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#delete-items","title":"Delete items","text":"<p>For deleting items you should use the way you use in lists and dicts: use <code>del UDict[key]</code> syntax:</p> <p>Example</p> <pre><code>del d['id']\n</code></pre> <p>You can use indexes and slices</p> <pre><code>del d[1]\ndel d[:] # all UDict will become empty\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#get-length-of-dict","title":"Get length of dict","text":"<p>You can get length of dict using inbuilt <code>len()</code> function</p> <pre><code>d = UDict(hello=1, hi=2)\nlen(d) # 2\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#iterate-dict-keys-values-items-properties","title":"Iterate dict: <code>keys</code>, <code>values</code>, <code>items</code> properties","text":"<p>You can iterate dict using <code>for key, value in UDict</code> syntax.</p> <pre><code>d = UDict(hello=1, hi=2)\nfor key, value in d:\n    print(key, value)\n\n# out:\n# hello 1\n# hi 2\n</code></pre> <p>Also, you can iterate <code>items</code> property for this result</p> <pre><code>for key, value in d.items:\n    ...\n</code></pre> <p>If you want iterate only all keys or values, use <code>keys</code> or <code>values</code> properties</p> <pre><code>for key in d.keys:\n    ...\n\nfor value in d.values:\n    ...\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#check-that-dict-is-empty-or-not-empty","title":"Check that dict is empty or not empty","text":"<p>You can use <code>is_empty()</code> method to check that UDict is empty</p> <pre><code>d = UDict()\nprint(d.is_empty()) # True\n\nd['hello'] = 'world'\nprint(d.is_empty()) # False\n</code></pre> <p>You also can use <code>if UDict</code> or <code>bool(UDict)</code> syntax</p> <pre><code>d = UDict()\nprint(bool(d)) # False\n\nd['hello'] = 'world'\nprint(bool(d)) # True\n\nif d:\n    print('True!')\n\n# out:\n# True!\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#check-that-key-or-item-in-dict","title":"Check that key or item in dict","text":"<p>You can check that key in dict</p> <pre><code>d = UDict(hi=1, hello=2)\n\nprint('hi' in d) # True\nprint('hii' in d) # False\n</code></pre> <p>You can also check that item in dict</p> <pre><code>d = UDict(hi=1, hello=2)\n\nprint(('hi', 1) in d) # True\nprint(('hi', 11) in d) # False\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#using-repr-and-print","title":"Using <code>repr()</code> and <code>print()</code>","text":"<p>Because <code>print()</code> uses <code>repr()</code>, you can directly print <code>UDict</code> objects</p> <pre><code>d = UDict(hi=1, hello=2)\nprint(d) # u{'hi': 1, 'hello': 2}\nprint(repr(d)) # u{'hi': 1, 'hello': 2}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#comparing-dicts","title":"Comparing dicts","text":"<p>You can compare <code>UDict</code>s using inbuilt compare operators (<code>==</code>, <code>!=</code>, <code>&gt;</code>, <code>&gt;=</code>, <code>&lt;</code>, <code>&lt;=</code>)</p> <p>Note</p> <p>When you use equal and not equal compare operator, dicts are comparing by its items and length, but in other compare operators dicts are comparing only by its length.</p> <p>For example, <code>d &gt; d2 -&gt; len(d) &gt; len(d2)</code>, etc.</p> <pre><code>d = UDict(hi=1, hello=2)\nd2 = UDict(hi=1, hello=2)\nd3 = UDict(hi=1, hello=2, world=3)\n\nprint(d == d2) # True\nprint(d != d2) # False\nprint(d &lt; d3) # True\nprint(d &lt;= d3) # True\nprint(d3 &gt; d) # True\nprint(d3 &gt;= d) # True\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#math-operations","title":"Math operations","text":"<p>You can use inbuilt math operators (<code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>+=</code>, <code>-=</code>, <code>*=</code>, <code>/=</code>) with <code>UDict</code>s</p> <p>Note</p> <p>When you use sum and sub math operators (<code>+</code>, <code>-</code>) dicts are summing or subtracting, but when you use other math operators dict will be multiplying or dividing by integer or dict.</p> <p>When dict it works like this:</p> <pre><code>d * {'hello': 2, 'hi': 0.5} -&gt; \nd['hello'] * 2\nd['hi'] * 0.5\n</code></pre> <pre><code>d = UDict(hi=1, hello=2)\nprint(d + {'world': 3}) # u{'hi': 1, 'hello': 2, 'world': 3}\nprint(d - {'hello': 2}) # u{'hi': 1}\n\nprint(d * 2) # u{'hi': 2, 'hello': 4}\nprint(d * {'hi': 2}) # u{'hi': 2, 'hello': 2}\n\nprint(d / 2) # u{'hi': 0.5, 'hello': 1}\nprint(d / {'hi': 2}) # u{'hi': 0.5, 'hello': 2}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#negative-dict","title":"Negative dict","text":"<p>You can use unary minus with dicts</p> <pre><code>d = UDict(hi=1, hello=2)\nprint(-d) # u{'hi': -1, 'hello': -2}\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#reverse-dict","title":"Reverse dict","text":"<p>You can reverse dict using <code>reverse()</code> or <code>reversed()</code> method</p> <p>When you use <code>reverse()</code>, dict updates in contrast to <code>reversed()</code>. Be careful!</p> <pre><code># reverse()\nd = UDict(b=1, a=2)\nprint(d.reverse()) # u{'a': 2, 'b': 1}\nprint(d) # u{'a': 2, 'b': 1}\n\n# reversed()\nd = UDict(b=1, a=2)\nprint(d.reversed()) # u{'a': 2, 'b': 1}\nprint(d) # u{'b': 1, 'a': 2}\n</code></pre> <pre><code>d.reverse()\nd.reversed()\n</code></pre> <p>You can use <code>~</code> operator and <code>reversed()</code> class. They are equivalents of <code>UDict.reversed()</code></p> <pre><code>~d\nreversed(d)\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/udict/#sort-dict","title":"Sort dict","text":"<p>You can use <code>sort()</code> and <code>sorted()</code> methods for sorting UDict</p> <p><code>sort()</code> is updating dict, <code>sorted()</code> - isn't. Be careful!</p> <pre><code># sort()\nd = UDict(b=1, a=2)\nprint(d.sort()) # u{'a': 2, 'b': 1}\nprint(d) # u{'a': 2, 'b': 1}\n\n# sorted()\nd = UDict(b=1, a=2)\nprint(d.sorted()) # u{'a': 2, 'b': 1}\nprint(d) # u{'b': 1, 'a': 2}\n</code></pre> <pre><code>d.sort()\nd.sorted()\n</code></pre>","tags":["useful class"]},{"location":"useful_classes/ustack/","title":"UStack[VT] (deprecated)","text":"<p>UStack is deprecated in version 0.2 and will be deleted in version 0.5. Use <code>uSTL.Stack</code> class instead, but it is also deprecated and will be deleted in version 0.5</p>","tags":["useful class"]},{"location":"useful_classes/ustack/#class-ustackvt","title":"class UStack[VT]","text":"","tags":["useful class"]},{"location":"useful_features/generators/","title":"Generators","text":"","tags":["useful feature"]}]}